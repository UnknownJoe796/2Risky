<Page
    x:Class="CS3450.TooRisky.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CS3450.TooRisky"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.TopAppBar>
        <CommandBar>
            <!--            <CommandBar.Content>
                <Grid>
                    ~1~<TextBlock Text="New Game" ToolTipService.ToolTip="Starts a new game. Leaves current game." FontSize="26.667" Foreground="{ThemeResource AppBarItemForegroundThemeBrush}"/>
                @1@
                    <ListView x:Name="PlayerList"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.HorizontalScrollMode="Enabled"                  
                                ScrollViewer.VerticalScrollMode="Disabled"
                             ItemsSource="{x:Bind ViewModel.Players}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <TextBlock Text="{Binding Name}" TextWrapping="NoWrap"/>
                                    <TextBlock Text="{Binding TotalUnits}" TextWrapping="NoWrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </CommandBar.Content>-->
            <AppBarButton x:Name="NewGameButton" Icon="NewWindow" Label="New Game" Click="NewGameButton_Click"></AppBarButton>
            <AppBarButton x:Name="ForfeitButton" Icon="Cancel" Label="Forfeit"></AppBarButton>
            <AppBarButton x:Name="GameLogButton" Icon="List" Label="Game Log" Click="HelpButton_Click"/>
            <AppBarButton x:Name="HelpButton" Icon="Help" Label="Help" Click="HelpButton_Click"/>

            <AppBarButton x:Name="AboutButton" Icon="Character" Label="About" Click="AboutButton_Click"/>
            <AppBarButton x:Name="EndTurnButton" Icon="Accept" Label="End Turn"/>
        </CommandBar>
    </Page.TopAppBar>

    <Grid x:Name="MainGrid" Background="{ThemeResource SystemChromeAltLowColor}">
        <Image x:Name="image" HorizontalAlignment="Left" Margin="0,47,0,0" VerticalAlignment="Top"  Source="Assets/RiskMap.png" Height="1033"/>
        
        
        <Grid x:Name="PlayerStats" HorizontalAlignment="Left" Height="363" Margin="0,609,0,0" VerticalAlignment="Top" Width="306" RenderTransformOrigin="-0.272,1.026" Background="LightGray">
        </Grid>

    </Grid>
</Page>
